# dynamic-memory-allocation-simulator
A C++ simulator for dynamic memory allocation with process-based memory management, supporting allocation, deallocation, reallocation, typed memory blocks, memory protection, and operation history logging.
